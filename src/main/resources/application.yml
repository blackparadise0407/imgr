server:
  port: ${PORT:8081}

imgr:
  app:
    jwtSecret: 040799Khoa
    jwtExpiration: 900000

spring:
  application:
    name: imgr
  security:
    filter:
      order: 10
    user:
      password: 040799
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/imgr}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:040799}
    driver-class-name: org.postgresql.Driver
  jpa:
    database: postgresql
    hibernate:
      ddl-auto: update
    show-sql: true


